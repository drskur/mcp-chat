apiVersion: v1
kind: Service
metadata:
  name: pace-mcp-host
  namespace: mcp-host
  labels:
    app: pace-mcp-host
spec:
  selector:
    app: pace-mcp-host
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP