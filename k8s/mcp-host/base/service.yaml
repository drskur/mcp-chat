apiVersion: v1
kind: Service
metadata:
  name: pace-mcp-host-api
  namespace: mcp-host
  labels:
    app: pace-mcp-host-api
spec:
  selector:
    app: pace-mcp-host-api
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP