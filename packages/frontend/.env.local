# 백엔드 API URL (서버 컴포넌트용)
API_URL=http://localhost:8000

# 클라이언트 사이드용 (브라우저에서 접근 가능)
NEXT_PUBLIC_API_URL=http://localhost:8000

# 개발 환경 설정
NODE_ENV=development

# 사용자 설정 디렉토리 (선택사항)
MCP_CLIENT_SETTINGS_DIR=.pace-mcp-client